# Normalize all text to LF in the repo (this trains/runs on Linux; CRLF breaks
# shell shebangs and dockerfile COPYed scripts).
* text=auto eol=lf
*.sh  text eol=lf
*.py  text eol=lf
*.dockerfile text eol=lf
